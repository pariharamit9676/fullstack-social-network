const PostCaption = ({ name, desc }) => (
    <div className="mt-2">
      <p className="text-gray-800">
        <strong>{name}</strong> {desc}
      </p>
    </div>
  );
  
  export default PostCaption;
  